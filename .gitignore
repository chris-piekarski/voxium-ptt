# Virtualenv (keep ignoring root-level venv/ for old trees; standard is .venv/)
.venv/
.venv-wsl/
.venv-*/
venv/
env/

# Editable / build metadata
*.egg-info/
__pycache__/
*.py[cod]
*$py.class
.Python
*.so

# Makefile dev stamp (pip install -e ".[dev]")
.dev-install-stamp

.codex

# Local tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
.cache/

# Voxium data (see voxium.paths; same layout on all platforms)
/models/
/logs/

# Repo-local llama.cpp prebuild (llama-server, DLLs, etc. — provisioned by setup or
# ``voxium models --polish --pull-polish``; platform-specific and large; same idea as models/)
tools/llama.cpp/

# IDE
.idea/

# OS
.DS_Store
Thumbs.db
